# BMad Game Dev Studio (GDS) module config
# GDS v0.7.2 skills read this file on activation (classic YAML shape; core uses _bmad/config.toml).
# Edit freely — values here override nothing in core; they are only read by gds-* skills.

# --- inherited from core ---
user_name: "Tae-jun"
communication_language: "한국어"
document_output_language: "한국어"
output_folder: "{project-root}/_bmad-output"

# --- GDS module ---
project_name: "SoloHero"
game_dev_experience: "intermediate"   # beginner | intermediate | expert
planning_artifacts: "{project-root}/_bmad-output/planning-artifacts"
implementation_artifacts: "{project-root}/_bmad-output/implementation-artifacts"
sprint_artifacts: "{project-root}/_bmad-output/implementation-artifacts"
project_knowledge: "{project-root}/docs"
primary_platform:
  - unity
